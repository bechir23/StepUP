# Google Colab already ships everything below, so on Colab you only need:
#     pip install pytorch-metric-learning
#
# Not preinstalled on Colab (install these):
pytorch-metric-learning>=2.0

# Optional
umap-learn      # nicer embedding projection for --plot-embed (falls back to sklearn t-SNE)
wandb           # only if you use --wandb online/offline
huggingface_hub # only if you use --hf-repo (preinstalled on Colab)

# Preinstalled on Colab (listed for local/non-Colab installs; uncomment if missing):
# torch>=2.1
# torchvision>=0.16
# numpy
# pandas
# pyarrow
# scikit-learn
# tqdm
# matplotlib
